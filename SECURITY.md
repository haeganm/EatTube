# Security

EatTube is a static Manifest V3 extension with no backend, no remote code, no credentials, and no API keys.

The extension requests no Chrome permissions and only runs as a content script on YouTube pages declared in `manifest.json`.

To report a security issue, open a private GitHub security advisory or contact the repository owner through the published repository profile.
